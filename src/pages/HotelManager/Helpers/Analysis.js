export const selectStyles = {
  control: (base) => ({
    ...base,
    borderRadius: "8px",
    borderColor: "#d1d5db",
    boxShadow: "none",
    minHeight: "40px",
  }),
  menu: (base) => ({
    ...base,
    borderRadius: "8px",
    overflow: "hidden",
  }),
};